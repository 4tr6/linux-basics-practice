cp nmap.txt /home/godz/desktop {copy file}
ctrl+L {cleear without delete}
mv nmap.txt + pass {move}
mv nmap.txt and then the other name {rename}
mkdir+ the name of the directory { create directory}
rmdir+ the name of the directory { remove directory}
rm -r+ the name of the directory { remove even if it's not empty}
rm *.+ txt {to delete all files that ends with txt}
locate + the name of the directory { find the directory or file}
find + the name of the directory { shows you everythinng inside the directory}
-------------------------------------------------------------------------
CHATGPT CAN EXPLAIN THIS FOR YOU WITH MORE DETAILS ❤
